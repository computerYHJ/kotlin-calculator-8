# 기능 구현 목록

### 오류체크
1. 숫자가 하나도 입력되지 않는 경우
2. 설정된 구분자 외의 다른 구분자가 입력된 경우

### 커스텀 구분자 설정
1. 정규식을 활용하여 기본 구분자 리스트에 추가
2. 입력받은 문자열에서 커스텀구분자 설정 문자열 제거

### 문자열 파싱
1. 구분자 리스트의 문자들을 하나의 문자로 통일
2. 입력된 문자열을 하나의 문자로 split 후 list생성

### 계산
1. 생성된 list값을 하나하나 정수로 변환하여 결과값에 추가